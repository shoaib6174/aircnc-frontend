import React from 'react';
import './Homepage.css'
import BookingForm from '../BookingForm/BookingForm';

const Homepage = () => {
    return (
        <div>
            <BookingForm/>
        </div>
    );
};

export default Homepage;